#pragma once
#include <qcolor.h>

class Constants
{
public:
	static int numberOfRowsCols;
	static int defaultMargin;
	static int defaultWidhHeight;
	static int startXPosition;
	static int startYPosition;
	static int viewWidth;
	static int viewHeight;
	static QColor defaultTextColor;
	static QColor lightBoxColor;
	static QColor darkBoxColor;
};

